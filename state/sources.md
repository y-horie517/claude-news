# 巡回する情報源

## 一次情報（必ず毎回確認する）

**WebFetch で実際に取れる URL を書いています。** 別のURLに変えないこと（GitHub の blob ページや docs.claude.com は中身が取れません）。

| 種別 | 取得に使う URL | 見るもの |
|---|---|---|
| Claude Code CHANGELOG | `https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md` | 前回チェック以降のバージョン差分。**速報の主軸**。記事中のリンクは `https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md` を使ってよい |
| Claude Code ベストプラクティス | `https://code.claude.com/docs/en/best-practices` | **「① ベストプラクティス」節の一次ソース。**「型」と「TIPS」もここから |
| Claude Code ドキュメント | `https://code.claude.com/docs/en/overview` ／ 目次は `https://code.claude.com/docs/llms.txt` | 新しいページ・記述の変化 |
| Claude Platform リリースノート | `https://platform.claude.com/docs/en/release-notes/overview` | モデル・API・Console の変更 |
| 導入事例 | `https://claude.com/customers` | 企業の導入事例（チーム展開の題材） |
| Anthropic ニュース | `https://www.anthropic.com/news` | 製品・モデルの発表 |
| Anthropic Engineering | `https://www.anthropic.com/engineering` | 使い方の設計論。**深掘り連載の一次ソースとして質が高い** |

## URL の書き方の注意

ネットワークは **Full**（全ドメイン許可）です。egress でブロックされることはありません。ただし URL の形によっては中身が取れません。

- `https://github.com/**/blob/**` — HTML の外枠しか返りません。必ず `raw.githubusercontent.com/<org>/<repo>/<branch>/<path>` に読み替える
- `https://docs.claude.com/...` — `code.claude.com/docs/...`（Claude Code）/ `platform.claude.com/docs/...`（API・Console）に 301 されます。最初から新URLで取る
- `https://www.anthropic.com/news` `https://www.anthropic.com/engineering` — 読めます（公式ニュース・技術記事の一次情報）

## 二次情報（事例集めに使う。仕様の根拠にはしない）

- WebSearch: `"Claude Code" tips` / `"Claude Code" workflow` （直近1週間に絞る）
- WebSearch 日本語: `Claude Code 使い方` / `Claude Code 事例` （Zenn / Qiita / はてなブログ）
- WebSearch: `site:reddit.com/r/ClaudeAI` の直近の話題
- Hacker News の Claude 関連スレッド

個別記事は WebFetch で**本文まで読んでから**書くこと。検索結果の抜粋だけで書いた場合は `unverified` として明示する。

## 前回チェック時点

エージェントは速報セクションを書いたあと、確認した CHANGELOG の最新バージョンをここに記録する。
次回はこのバージョンより新しいものだけを「速報」として扱う。

- last_changelog_version: v2.1.280
- last_checked_at: 2026-09-23（第032号。CHANGELOG に v2.1.280 が追加された（v2.1.279 は欠番）。最大の項目は Claude Opus 5.5（`claude-opus-5-5`）の追加で、既定の Opus モデルになった。1M コンテキスト・出力上限128k・$4／$20 per MTok・キャッシュ読み $0.20/MTok。Pro と Team Standard の既定モデルも Sonnet から Opus に変わった。`/effort` の保存値が新モデルに引き継がれなくなった点は①の主題にした。ほかに `CLAUDE_CODE_MAX_MCP_DESCRIPTION_LENGTH`、`hook_execution_complete` への出力サイズ追加、シンボリックリンク経由の書き込みをリンク先で判定する修正、auto mode の再試行暴走2件の修正、ダイアログの Ctrl+C/D 二度押しの修正、`y`/`n` 誤操作の廃止など項目数は非常に多い。anthropic.com/news は 9/22 に「Introducing Claude Opus 5.5」が追加（多くの作業で Fable 5.1 と同水準、実行費用は Opus 5 より40%安い）。Platform リリースノートも 9/22 に Opus 5.5 と、会話途中のツール定義ベータ（`inline-tools-2026-09-15`）が追加された。anthropic.com/engineering は 4/23 のまま、claude.com/customers は日付が表に出ず Impel（9/16）より新しいものを確認できなかった。①は `code.claude.com/docs/en/model-config` を一次情報にし、`modelSettings`・`maxEffortLevel`・`fallbackModel` とモデル別の既定労力（Opus 5.5 は medium）を使った。設定ファイルの優先順位そのものは第022号で既報なのでリンクだけにした。②の連載はカリキュラム26番で、`code.claude.com/docs/en/desktop` の `.claude/launch.json` と `autoVerify`、`code.claude.com/docs/en/skills` の `/run`・`/verify`・`/run-skill-generator`、`best-practices` の UI 検証の行、`chrome` ページを一次情報にした。Chrome 連携そのものは第022号で既報なので4手段の比較表の一行に留めた。④は customers に新規が無いため二次情報2本（Zenn / yusuke_shiya・SmartHR 2026-02-16、Zenn / ENECHANGE 2026-03-23）で構成し、いずれも日付を明示した。ENECHANGE の MCP 対 CLI のトークン数は記事の主張で公式計測ではない旨を本文に書いた。`code.claude.com/docs/llms.txt` は今回も `/en` なしで取得できた。Artifact は既存 URL への更新で Version 27 になった）
- last_checked_at: 2026-09-22（第031号。CHANGELOG に新しい版はなく v2.1.278 のままだった。Platform リリースノートは 9/18 の Compliance API が最新、anthropic.com/news は 9/18 の Accenture 提携のまま、anthropic.com/engineering は一覧の先頭が Featured の「How we contain Claude across products」で日付つきの最新は 4/23 のまま、claude.com/customers も Impel（9/16・第026号で既報）より新しいものは無かった。よって⑤速報は「本日は特筆すべき発表はありませんでした」とし、確認先と最終更新の表だけを載せた。①は新規トピックの `code.claude.com/docs/en/claude-security`（claude-security プラグイン）を一次情報にした。第025号の `security-guidance` とは別物なので、防御5層の表で位置づけを明示している。`enabledPlugins` の形は `settings-reference` で確認した。アンチパターンは「差分スキャンはコミット済みだけを読む」を使った。②の連載はカリキュラム25番で、`code.claude.com/docs/en/github-actions` を一次情報にし、ワークフロー2本をドキュメントの例のまま載せた。`/install-github-app` の挙動は `commands` ページで裏を取っている。③は `scheduled-tasks`（`.claude/loop.md`・ジッタ・7日期限）と `commands`（`/config key=value`）から拾った。④は customers に新規が無いため二次情報2本（Zenn / morisawa・ST. 2026-02-27、ClassLab 2026-04-20）で構成し、いずれも日付を明示した。ClassLab の記事にある「レビュー時間74%短縮」は本文ではなく別記事の引用だったため採らず、計測の仕組みと目安の数値だけを使った。`code.claude.com/docs/en/llms.txt` は 404 で、正しくは `code.claude.com/docs/llms.txt`（`/en` なし）。Artifact は既存 URL への更新で、先に `action: "read"` で現行版を確認してから公開し Version 26 になった。更新公開では `prompts/daily.md` Step 3 の「favicon は 🌅 固定」は今回も適用していない）
- 前回: 2026-09-21（第030号。CHANGELOG に新しい版はなく v2.1.278 のままだった。Platform リリースノートは 9/18 の Compliance API が最新、anthropic.com/news は 9/18 の Accenture 提携のまま、anthropic.com/engineering は一覧の先頭が 4/23 のまま、claude.com/customers も Impel（9/16・第026号で既報）より新しいものは無かった。よって⑤速報は「本日は特筆すべき発表はありませんでした」とし、確認先と最終更新の表だけを載せた。①は新規トピックの `code.claude.com/docs/en/advisor`（アドバイザツール）を一次情報にした。`/advisor` は第025号までの速報で2回触れただけで、BP としては初回。モデルの組み合わせ表と、「拒否＝付けずに走る／API エラー＝毎リクエスト失敗」の違いをアンチパターンに使った。②の連載はカリキュラム24番で、`common-workflows` の Understand new codebases、`best-practices` の Ask codebase questions と Use subagents for investigation、`large-codebases` の Choose where to start Claude、`memory` の書き足す4基準を一次情報にした。`large-codebases` の設定群（`claudeMdExcludes`・`worktree.sparsePaths`・code intelligence）は第019号・第025号で既出のため、今回は読む順番と起動場所の話だけに絞っている。③は `common-workflows` の @ 参照の作法2本と `cli-reference` の `--no-session-persistence`、`commands` の `/insights`。④は customers に新規が無いため、二次情報1本（Zenn / monokaai・Spectee、2026-03-17、4週間の実測値つき）と claude.com/blog の大規模コードベース記事（2026-05-14）で構成した。Artifact は既存 URL への更新で、先に `action: "read"` で現行版を確認してから公開し Version 25 になった。更新公開では `prompts/daily.md` Step 3 の「favicon は 🌅 固定」は今回も適用していない）
- 前回: 2026-09-20（第029号。CHANGELOG に v2.1.278 が追加された。項目は3件で、Claude API と Enterprise の Bedrock / Vertex / Foundry / ゲートウェイ利用者で auto mode の分類器が既定でサーバー側実行になる変更（解除は `CLAUDE_CODE_AUTO_MODE_SERVER=0`）、`/status` の「Auto mode server」行の追加、分類器のオーバーヘッドが課金されるフォールバック時の警告。Platform リリースノートは 9/18 の Compliance API が最新で前号から動きなし、anthropic.com/news は 9/18 の Accenture 提携のままで新規なし、anthropic.com/engineering も一覧の先頭が 4/23 のままで新規なし、claude.com/customers も Impel（9/16・第026号で既報）より新しいものは無かった。①は新規トピックの `code.claude.com/docs/en/auto-mode-config` を一次情報にした。auto mode そのものは第4回（2026-08-31）で扱っているため、今回は `autoMode.environment` の設定側に寄せ、`$defaults` を書かずに配列を置き換えると組み込みルールを丸ごと捨てる点をアンチパターンにした。v2.1.278 の速報と地続きになる。②の連載はカリキュラム23番で、`agents`（Run agents in parallel の5手段比較表）・`agent-view`・`interactive-mode` の Background Bash 節を一次情報にした。`/batch` と `claude agents` は 2026-08-30／09-01 の号で TIPS として既出のため、今回は「どれに逃がすかの判断」と背景 Bash の停止条件に寄せている。④は customers に新規が無いため、一次情報1本（anthropic.com/engineering の C コンパイラ、2026-02-05）と二次情報1本（Zenn / 濱田優貴、2026-07-30、5社ロールアウト）で構成した。engineering の一覧ページからリンクを推測して 404 を踏んだので、正しい URL は WebSearch で確かめてから使っている（正: `https://www.anthropic.com/engineering/building-c-compiler`）。Artifact は既存 URL への更新で、先に `action: "read"` で現行版を確認してから公開し Version 24 になった。更新公開では `prompts/daily.md` Step 3 の「favicon は 🌅 固定」は今回も適用していない）
- 前回: 2026-09-19（第028号。CHANGELOG に v2.1.276 と v2.1.277 が追加された。v2.1.276 は `ANTHROPIC_BASE_URL` がプロキシ・ゲートウェイを指すと全リクエストが 400 になる不具合の修正1件のみ。v2.1.277 は項目数が非常に多いが、速報には `CLAUDE.md` の無いプロジェクトでの `AGENTS.md` 対応、`CLAUDE_GATEWAY_PROXY_IS_EGRESS_BOUNDARY=1` と gateway upstream の `headers:`、`--worktree` セッションでプロジェクトスキルが読まれない不具合の修正、作業中に打ったメッセージが無視される不具合の修正、`TaskOutput` ツールの削除と `claude -p` の Haiku 自動タイトル取得の削除、クラウド環境の Personal/Organization 分離と組織側の読み取り専用化（管理設定の「Web」は「Cloud sessions」に改称）を拾った。Platform リリースノートは 9/18 の Compliance API が最新で前号から動きなし。anthropic.com/news は 9/18 に「Partnering with Accenture on embedded evaluation」が追加されたが開発者向けの機能変更ではないため速報の表に1行だけ置き、記事 URL は推測せず一覧ページを出典にした。anthropic.com/engineering は更新なし。claude.com/customers の先頭は Notion・Slack・Figma・HubSpot だがこれは注目枠で、Impel（9/16・第026号で既報）より新しい事例は無く、Pictet も 2026-09-03 の号で既報のため、④は二次情報2本（Zenn / kuma8088 2026-01-25、Zenn / Shu Hirouchi 2026-03-23）で構成した。①は新規トピックの `code.claude.com/docs/en/ultrareview` を一次情報にした（`/ultrareview --post` は第021号の速報で触れただけで、BP としては初回）。best-practices の「Add an adversarial review step」と繋げ、`claude ultrareview` を呼ぶ `scripts/pre-merge-review.sh` と `package.json` を載せた。②の連載はカリキュラム22番のデバッグで、`common-workflows` の Fix bugs efficiently と `best-practices` の Describe the symptom／Address root causes／訂正2回で `/clear` を一次情報にした。③は `memory`（AGENTS.md）と `debug-your-config`（`--safe-mode`・`/debug`）から拾った。Artifact は既存 URL への更新で、先に `action: "read"` で現行版を確認してから公開し Version 23 になった。CHANGELOG v2.1.275 で Artifact ツールの favicon が一語の `icon` に変わったため、更新公開では `prompts/daily.md` Step 3 の「favicon は 🌅 固定」は今回も適用していない）
- 前回: 2026-09-18（第027号。CHANGELOG に v2.1.274 と v2.1.275 が追加された。どちらも項目数は非常に多いが修正が主体で、速報には送信キー ctrl+enter（`ctrl+x ctrl+s`）、`syncClaudeAiSkills`/`syncClaudeAiPlugins`、`/plugin install --marketplace <source>`、npm 由来プラグインの `npm pack --ignore-scripts` ＋整合性検証、`/update-config` が `Write(path)` ではなく `Edit(path)` を書く修正、`CLAUDE_CODE_MCP_STARTUP_WAIT_MS`、`/code-review` が軽いインラインレビューに変わった件、クラウドの差分表示の比較ブランチ選択、`/goal` の修正2件を拾った。Platform リリースノートは 9/18 に「Compliance API がローカルセッション端点で Claude in Chrome（`claude_in_chrome`）の記録を返す」（Enterprise ベータ）が追加されたので速報に入れた。anthropic.com/news は 9/17 に Life Sciences Verification Program が追加されたが開発向けではないため採らなかった。anthropic.com/engineering の一覧の先頭に「How we contain Claude across products」（2026-05-25）が現れたが、3層の封じ込めという枠組みは第011号（2026-08-31）の型で既出のため使わなかった。claude.com/customers は 9/16 の Impel が最新で第026号で既報のため、④は二次情報2本（Zenn / ARMテックブログ 梅津 2026-06-16、Qiita / とんとんぼ 2026-03-06）で構成した。①は新規トピックの `code.claude.com/docs/en/deep-links`（`claude-cli://` ディープリンク）を一次情報にした。②の連載はカリキュラム21番の Git 運用で、`worktrees`・`permissions`・`common-workflows`・`checkpointing`・`best-practices` を一次情報にした。worktree 自体は第019号（2026-09-09）の BP で扱っているため、今回はブランチの生成・終了時の後片付け・同名再利用時のリセット条件という git のライフサイクル側に寄せた。なお Artifact の公開は既存 URL への更新で、初回は「live version を見ていない」として拒否されたので `action: "read"` で現行版を確認してから再公開し、Version 22 になった。CHANGELOG v2.1.275 で Artifact ツールの favicon が絵文字から一語の `icon` に変わったが、更新公開では省略するため `prompts/daily.md` Step 3 の「favicon は 🌅 固定」は今回適用していない）
- 前回: 2026-09-17（第026号。CHANGELOG に新しい版はなく v2.1.273 のままだった。Platform リリースノートは 9/14 の Messages API の `compaction` が最新で第025号で既報、anthropic.com/news は 9/10 の脅威レポートのまま、anthropic.com/engineering は 4/23 のまま。claude.com/customers には 9/16 に Impel が追加されており、これを④の一次情報に使った。よって⑤速報は「特筆すべき発表なし」とし、確認先と最終更新の表だけを載せた。①は新規トピックの `code.claude.com/docs/en/channels` と `channels-reference`（チャンネル＝イベントを押し込む MCP サーバー、研究プレビュー）を一次情報にし、リファレンスの webhook receiver のコードをそのまま載せた。②の連載はカリキュラム20番のコードレビューで、`code.claude.com/docs/en/code-review` を一次情報にした。第6回（2026-09-02）でローカルの `/code-review` を扱っているため、今回はマネージドの Code Review（GitHub の PR に付く研究プレビュー、Team/Enterprise）との対照とチーム運用に寄せた。④の2本目は二次情報（Zenn / Canly ふくだ、2026-03-10 と 2026-07-13）で、マネージド版のコスト実測がドキュメント記載の平均15〜25ドルと開いている点を本文で明示した。直近1週間の新しい日本語の実践記事は今日も薄い）
- 前回: 2026-09-16（第025号。CHANGELOG に v2.1.272 と v2.1.273 が追加された。v2.1.272 は「バグ修正と信頼性改善」の1行のみ。v2.1.273 は項目数が非常に多いが新機能より修正が主で、MCP 切断時の通知、`--remote-control` セッションのフォーク、長いセッションで会話全体を再処理しない改善、コンテキストメーターが advisor の手番を約2倍に数えていた修正、`/login`・`/upgrade`・`/extra-usage` によるプロンプトキャッシュの全書き直しの修正、`REVIEW.md` が @メンションや行をまたぐコードスパンで無視される修正、`OTEL_LOG_TOOL_DETAILS=1` がエージェント・スキル・プラグイン・MCP の実名を出す変更、`permissions.blockReadsOutsideWorkingDirectories` 周辺3件、2.1.268 の deny 判定変更のリバートを速報で扱った。Platform リリースノートは 9/14 に「Messages API On-Demand Compaction」（`compaction` パラメータ、ベータ `compact-2026-09-04`）が追加されていたので速報に入れた。anthropic.com/news は 9/10 の脅威レポートが最新で既報、anthropic.com/engineering は 4/23 のまま、claude.com/customers も 9/4 の Carvana が最新で既報。①は新規トピックの `code.claude.com/docs/en/security-guidance`（security-guidance プラグイン）を一次情報にした。②の連載はカリキュラム19番の大規模リファクタで、`claude.com/blog/ai-code-migration`（2026-07-16、Anthropic の6手順）と新規ページ `code.claude.com/docs/en/large-codebases`、`common-workflows` を一次情報にした。④は claude.com/customers に新規がないため、一次情報の移行ブログ1本と二次情報1本（Zenn / team_zenn dyoshikawa、2025-12-08）で構成した。直近1週間の日本語の実践記事は今日も薄く、検索上位はマーケティング系メディアが多かったため採らなかった。③の TIPS は large-codebases と common-workflows から `worktree.symlinkDirectories`・`CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD`・`claude --from-pr` を拾った。なお Artifact の公開は既存の `daily` URL を渡して同じページを更新し、返ってきた正規URL `https://claude.ai/artifact/Up2T56AqoW7TDJ6m8589N2`（Version 20）を state に記録した。重複した Claude Daily は作られていない）
- 前回: 2026-09-15（第024号。CHANGELOG に v2.1.271 が追加された。項目数が非常に多い回で、サブエージェントのフロントマターと `--agents` JSON の `omitClaudeMd`、auto mode ＋サンドボックスでの Bash/PowerShell/Monitor へのコマンド単位 `allowed_domains`、`claude plugin install/update` の `--accept-command <sha256>`、Bash 権限検査の抜け穴修正が複数（ワイルドカードの展開先・`fmt`/`column` が読むファイル・変数宣言フラグによる偽装・二重 `cd`/サブシェル経由の迂回）、Claude Code Remote セッションでの fast mode、コンパクション後の背景コマンド二重起動の修正、Monitor のウォッチが必ず期限付きに（最長30分・`-p` では10分、無期限の `persistent` は廃止）、ダイナミックワークフローが使用量上限で一時停止して自動継続、ワークフローの既定サイズが Pro で `small`・`medium` が15→10体、`--resume` の修正複数（`[1m]` の消失・`/artifacts` 添付の消失・前の会話のファイル読み取り追跡の持ち越し）などを速報で扱った。Platform リリースノートは 9/10 の Managed Agents `auto` と `ant beta:sessions connect` のまま新規なし、anthropic.com/news は 9/1 の「Developing Enterprise Frontier Safeguards with our customers」が最新、anthropic.com/engineering は 4/23 のまま、claude.com/customers も 9/4 の Carvana が最新で既報。①は新規トピックの `code.claude.com/docs/en/workflows`（ダイナミックワークフロー）を一次情報にした。ドキュメントの size guideline の表は `medium` を「15体未満」と書いたままで CHANGELOG の 10 体と食い違っており、本文で明示した。②の連載はカリキュラム18番のテスト駆動で、`best-practices`（The trust-then-verify gap・検証の4段・Writer/Reviewer）・`goal`・`common-workflows` を一次情報にした。④は claude.com/customers に新規がないため二次情報2本（Zenn / 井本賢 2026-02-24、Zenn / soso（Gemcook）2026-03-12）で構成し、いずれも日付を明示した。直近1週間の新しい実践記事は日英とも薄い）
- 前回: 2026-09-14（第023号。前回チェック以降、巡回先のいずれにも新規の更新がなかった。CHANGELOG は v2.1.270 のままで新しいバージョンなし、Platform リリースノートは 9/10 の Managed Agents `auto` と `ant beta:sessions connect` のまま、anthropic.com/news は 9/10 の脅威レポートのまま、anthropic.com/engineering は 4/23 のまま。claude.com/customers も 9/4 の Carvana が最新で既報。よって⑤速報は「本日は特筆すべき発表はありませんでした」とし、確認先と最終更新日の表だけを載せた。②の連載はカリキュラム17番の利用形態の使い分けで、`code.claude.com/docs/en/platforms`・`feature-availability`・`remote-control`・`claude-code-on-the-web` を一次情報にした。①は `code.claude.com/docs/en/cloud-environments`（setup script と SessionStart フックの分担、`CLAUDE_CODE_REMOTE`、環境キャッシュ、届く/届かないの表）が一次情報。④は claude.com/customers に新規がないため二次情報2本（Qiita / @kai_kou 2026-07-12、Zenn / ナレッジワーク 2026-03-06）で構成した。後者はプラン単価に触れているが、料金は変わりうるため金額を本文に書かず「優先順位を先に決めてからプランを選ぶ」という判断の型だけを引いた）
- 前回: 2026-09-13（第022号。CHANGELOG に v2.1.270 が追加された。項目は1件だけで、「セッションを長く走らせたあとに読み取り専用の git コマンドが許可を求めるようになっていた v2.1.269 の回帰」の修正。新規版が薄いぶん、前号で扱いきれなかった v2.1.269 の項目から読者に効くもの（日本語・中国語・タイ語など単語間に空白を置かない言語でプロンプト候補が落ちていた修正、`Edit()` の deny ルールと書き込みパス検査が Bash `tee` の書き込み先にも適用されるようになった件、`--output-format stream-json` の `permission_denials` がパス指定 deny の Read/Edit/Write を落としていた修正、claude.ai 同期スキルの `anthropic-skills:<name>` 命名、プラグインアーカイブが他ローカルユーザーから読めていた問題の修正、VS Code の Hooks ダイアログと Permission rules ダイアログ）を速報に回した。Platform リリースノートは 9/10 の Managed Agents `auto` と `ant beta:sessions connect` のまま新規なし、anthropic.com/news も 9/10 の脅威レポートのまま、anthropic.com/engineering は 4/23 から更新なし。claude.com/customers は 9/4 の Carvana が最新で既報のため、④は二次情報2本（Zenn / PURPM MEDIA LAB 2026-06-30、Zenn / dress_code 2026-04-27）で構成した。直近1週間の新しい実践記事は日本語・英語とも薄く、Chrome 連携というテーマに合う既存記事を日付を明示して使っている。②の連載はカリキュラム16番の settings.json で `code.claude.com/docs/en/settings` を一次情報にし、①は `code.claude.com/docs/en/chrome` と `code.claude.com/docs/en/best-practices` が一次情報）
- 前回: 2026-09-12（第021号。CHANGELOG に v2.1.269 が追加された。項目数が非常に多い回で、`claude plugin eval`（プラグインのeval実行・JSON/HTMLレポート）、`/output-style [name]` の追加、`bashEditDiffEnabled`、`OTEL_METRICS_INCLUDE_REPOSITORY`、`CLAUDE_CODE_GATEWAY_MODEL_DISCOVERY_TIMEOUT_MS`、`CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS`(1-256)、`/focus` のスピナーTIP、`!` 始まりの deny/ask ルールが書いたソース外に及んでいた不具合の修正（裸の `!` は無視）、コンパクション後の git status を現在値に修正、プロンプトキャッシュ破壊2件の修正、クラウドの定期実行（routine）の二重起動とサブエージェント使用時の早期完了扱いの修正、`/ultrareview --post` が直接PRにコメントする変更などを速報で扱った。Platform リリースノートは 9/10 の Managed Agents `auto` と `ant beta:sessions connect` のままで新規なし、anthropic.com/news も 9/10 の「Detecting and countering misuse of AI」のまま、anthropic.com/engineering は 4/23 から更新なし。claude.com/customers は 9/4 の Carvana が最新で、未報告だった 9/3 の Qonto を④の一次情報として使った。②の連載はカリキュラム15番の Artifacts で、`code.claude.com/docs/en/artifacts` を一次情報にした。①は新設の `code.claude.com/docs/en/plugin-evals` が一次情報。なお docs の Output styles ページには「`/output-style` は v2.1.91 で削除」の注記が残っており、CHANGELOG と食い違っている）
- 前回: 2026-09-11（第020号。CHANGELOG に v2.1.268 が追加された。項目が非常に多く、`/plugin` メニューを閉じた時点でインストール・有効化・無効化が反映されるようになった変更、`claude plugin install/uninstall/update/enable/disable` への `--json` 追加と `list --json` の `errorDetails`/`noteDetails`、WebFetch の300秒デッドライン（`CLAUDE_CODE_WEBFETCH_DEADLINE_MS`）、`ANTHROPIC_BASE_URL` 経由で全ターンが HTTP 400 になっていた v2.1.265 以降の回帰修正、シンボリックリンクディレクトリと `env -C`/`eval` 同一行での deny すり抜け修正、git URL のトークンと `${VAR}` 由来シークレットの表示修正、`CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS` の不発修正、ゲートウェイ関連の `gatewayInternalNetworks` などを速報で扱った。Platform リリースノートは 9/10 に更新があり、Managed Agents の権限ポリシー `auto`（`evaluation` フィールド）と `ant beta:sessions connect`（`--web`）を扱った。anthropic.com/news も 9/10 に「Detecting and countering misuse of AI: September 2026」が追加され1行で触れた。anthropic.com/engineering は 4/23 から更新なし。claude.com/customers は 9/4 の Carvana が最新で既報のため、④は二次情報2本（Yappli Tech Blog / Qiita）で構成した）
- 前回: 2026-09-10（第019号。CHANGELOG に v2.1.266 と v2.1.267 が追加された。v2.1.266 は `CLAUDE_CODE_USE_GATEWAY` が単独で Cloud ゲートウェイのサインインを強制していた v2.1.265 の回帰修正1件のみ。v2.1.267 は項目が非常に多く、`maxEffortLevel` の追加、`--system-prompt-snapshot off` の追加、MCP／コネクタ／`/model` 切替／セッション再開に起因するプロンプトキャッシュ破壊の修正が10件以上、マーケットプレイスのバックスラッシュによる containment 検査回避の修正、5MB 超セッション再開で並列ツール呼び出しとフック出力が落ちる不具合の修正などを速報で扱った。Platform リリースノートは 9/3 の ant CLI v1.30.0 と Google Cloud のper-message effort ベータのまま、anthropic.com/news は 9/1 の Fable 5.1 / Mythos 5.1 のまま、anthropic.com/engineering も 4/23 から更新なし。claude.com/customers は 9/4 の Carvana が最新で既報）
- 前回: 2026-09-09（第018号。CHANGELOG に v2.1.265 が追加された。項目は多数で、`--plugin-dir` のフォルダ指定と動的読み込み、プロンプトキャッシュ再利用の破壊修正3件、`--worktree` 起動の並列チェックアウト高速化、`/model opus[1m]` の拒否修正、ツール結果のディスク保存1GB上限、プラグインパスのバックスラッシュによるシンボリックリンク検査回避の修正などを速報で扱った。v2.1.264 は CHANGELOG に項目なし。Platform リリースノートは 9/3 の ant CLI v1.30.0 のまま、anthropic.com/news は 9/1 の Fable 5.1 / Mythos 5.1 のまま、anthropic.com/engineering も 4/23 から更新なし）
- 前回: 2026-09-08（第017号。CHANGELOG は v2.1.263 のままで新規バージョンなし。Platform リリースノートは 9/3 の ant CLI v1.30.0 が最新、anthropic.com/news は 9/1 の Fable 5.1 / Mythos 5.1 が最新、anthropic.com/engineering も 4/23 から更新なし。いずれも既報のため速報は「該当なし」で執筆）
- 前回: 2026-09-07（第016号。CHANGELOG は v2.1.263 のみ新規で、中身は「Bug fixes and reliability improvements」の1行のみ。v2.1.262 は CHANGELOG に項目がない。Platform リリースノートは 9/3 の ant CLI v1.30.0 から更新なし。anthropic.com/news は 9/1 の Fable 5.1 / Mythos 5.1 から新規なし。anthropic.com/engineering も 4/23 が最新で新規なし）
